function WordCount(str) {var totalSoFar = 0; for (var i = 0; i < WordCount.length; i++)
    if (str(i) === " ") {
      totalSoFar = +1;}
  totalsoFar += 1;}

console.log(WordCount("Random String"));

var wordAvg = 0;
  for (var i = 0; i < wordCount.length; i++){
    wordAvg += wordArray[i];} var avgLen = wordAvg / wordCount;
    console.log(("Words: " + wordArray[0]), "Character count: " + charCount, "Word count: " + wordCount, "Whitespace count: " + whiteSpace, "Word length average: " + avgLen);};

    str.length - str.replace(/\s+/g, '').length;
